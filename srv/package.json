{
  "name": "orders-srv",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "cds-serve"
  },
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4",
    "@sap/xssec": "^4",
    "@cap-js/postgres": "^2"
  },
  "cds": {
    "requires": {
      "db": { "kind": "postgres" },
      "[production]": { "auth": "xsuaa" }
    }
  }
}
